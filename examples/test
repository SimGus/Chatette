%[test]
  slot\: @[slot]

@[slot]
    ~[first] = HSM
    ~[second] = CRM

~[first]
    the line ~[line name#HSM]
~[second]
    the line ~[line name#CRM]

~[line name#HSM]
    HSM
    hot strip mill
    {pouet/test}
~[line name#CRM]
    CRM
    cold rolling mill
